**Phase 3: Token Verification**

- [← Back to Authentication Flowchart](_AuthenticationFlowchart.md)
- [← Previous: Phase 2: User Login](2.User%20Login.md)
- [Next → Phase 4: Protected Routes](4.Protected%20Routes.md)

```mermaid
sequenceDiagram
    autonumber
    actor User
    %% File paths:
    %% UserContext: client/src/context/UserContext.jsx
    %% verifyToken: server/src/middleware/authVerify.js
    %% getMe: server/src/controllers/user.js
    participant UserContext as UserContext Provider<br/>UserContext.jsx
    participant Server as Server Middleware<br/>authVerify.js
    participant Controller as User Controller<br/>user.js

    Note over User,Controller: Phase 3: Token Verification
    Note over UserContext,Controller: Occurs on app initialization<br/>and protected route access

    UserContext->>UserContext: useEffect triggers on mount
    activate UserContext
    UserContext->>Controller: GET /api/users/me<br/>credentials: "include"
    activate Controller

    Controller->>Server: verifyToken middleware executes
    activate Server

    Server->>Server: Extract token from httpOnly cookie
    Server->>Server: Verify JWT signature and expiration<br/>using jwt.verify()
    Server->>Server: Check token against blacklist<br/>(revoked tokens)

    alt Token invalid / missing / blacklisted
        Server->>Server: Check if route is `/api/jobs/search`
        alt Is Job Search
            Server->>Server: Set `req.user = null` (Guest Mode)
            Server->>Controller: Call next()
        else Not Job Search
            Server->>Controller: Return 401 error
        end
    else Token valid
        Server->>Server: Attach decoded user data<br/>to `req.user` object
        Server->>Controller: Call next() middleware
        deactivate Server

        Controller->>Controller: getMe function executes
        Controller->>Controller: Query database for user profile<br/>using req.user.id
        Controller->>Controller: Fetch user favorites
        deactivate Controller

        Controller->>UserContext: Return user data<br/>(success: true, user: userData)
        activate UserContext
        UserContext->>UserContext: Dispatch "LOGIN" action<br/>with user data
        UserContext->>UserContext: Normalize skills and favorites
        deactivate UserContext
        Note over UserContext,User: User authenticated and session restored
    end
```

## Key Components & Files

### Client Side

- **UserContext.jsx** (`client/src/context/UserContext.jsx`)
  - Automatic token verification on app mount
  - Session restoration from stored cookies
  - Global authentication state management

### Server Side

- **verifyToken** (`server/src/middleware/authVerify.js`)
  - JWT token verification middleware
  - Token blacklist checking
  - User data attachment to request object

- **getMe** (`server/src/controllers/user.js`)
  - Current user profile retrieval
  - User favorites fetching
  - Complete user data assembly

## Token Verification Process

### 1. Token Extraction

- Extracts JWT from httpOnly cookie
- Handles missing token scenarios

### 2. JWT Verification

- Verifies token signature using JWT_SECRET
- Checks token expiration
- Validates token structure

### 3. Blacklist Check

- Checks token against in-memory blacklist
- Prevents access with revoked tokens
- Handles logout scenarios

### 4. User Data Retrieval

- Uses decoded user ID from token (`req.user.id`)
- Queries database for complete user profile
- Includes user favorites and preferences

## Security Features

- **JWT Verification**: Cryptographic signature validation
- **Token Expiration**: Automatic session timeout
- **Blacklist Mechanism**: Immediate token revocation on logout
- **HttpOnly Cookies**: Prevents client-side token access
- **Secure Middleware**: Centralized authentication logic

## Error Handling

- **401 Unauthorized**: No token, invalid token, expired token, blacklisted token
- **500 Internal Server**: Database connection errors
- **Silent Failures**: Graceful handling of expected "No token provided" for guests

## Token Blacklist

- **Storage**: In-memory array of revoked tokens
- **Addition**: Tokens added on logout
- **Persistence**: Lost on server restart (acceptable for current implementation)
- **Performance**: O(n) lookup, suitable for moderate user counts

## User Data Structure

The verification response includes complete user profile:

```javascript
{
  id: string,
  email: string,
  first_name: string,
  last_name: string,
  avatar: string | null,
  street: string | null,
  house_number: string | null,
  city: string | null,
  country: string | null,
  skills: string[],
  favorites: Job[]
}
```
