**Phase 4.3.3: Not Cached for Full String**

- [← Back to Job Fetch Flowchart](_JobFetchFlowchart.md)
- [← Previous: Phase 4.3.1: Cache Check for Full String](4.3.1.Cache%20Check%20for%20Full%20String.md)
- [Next → Phase 4.5: Background Scraper Fetch (if authenticated)](4.5.Background%20Scraper%20Fetch.md)
- [Next → Phase 6: Display Results (if not authenticated)](6.Display%20Results.md)

```mermaid
sequenceDiagram
    autonumber
    %% File paths:
    %% JobController: server/src/controllers/jobData.js
    %% getCachedJobsBySearchString: server/src/services/getCachedJobsBySearchString.js
    %% apifyScraperFetchPersister: server/src/services/apifyScraperFetchPersister.js
    %% rapidAPIfetchPersister: server/src/services/rapidAPIfetchPersister.js
    %% rapidAPIfetch: server/src/services/rapidAPIfetch.js
    %% processRapidAPIjob: server/src/util/processRapidAPIjob.js
    %% persistJobSearch: server/src/services/persistJobSearch.js
    participant JobController as Job Search Controller<br/>jobData.js
    participant CacheService as getCachedJobsBySearchString
    participant ApifyPersister as apifyScraperFetchPersister
    participant RapidAPIPersister as rapidAPIfetchPersister
    participant RapidAPIService as rapidAPIfetch
    participant ProcessRapidAPI as processRapidAPIjob
    participant PersistJobSearch as persistJobSearch (async)

    Note over JobController,PersistJobSearch: Phase 4.3.3: Not Cached for Full String
    activate JobController
    Note over JobController: is_whole_string was false from Phase 4.3.1

    JobController->>ApifyPersister: apifyScraperFetchPersister(search_string, false, is_auth)
    Note over ApifyPersister: Triggers background Apify scrape only if authenticated

    JobController->>JobController: searchWords = search_string.split(/\s+/).filter(Boolean)
    loop For each searchWord
        JobController->>CacheService: getCachedJobsBySearchString(client, searchWord, is_auth)
        alt cache hit
            CacheService-->>JobController: cachedJobsPerSearchString
            JobController->>JobController: add cached jobs to aggregatedJobs (dedupe by id)
        else cache miss
            CacheService-->>JobController: []
            JobController->>RapidAPIPersister: rapidAPIfetchPersister(searchWord, is_auth)
            Note over RapidAPIPersister: Code calls without the in-progress tracker
            activate RapidAPIPersister
            RapidAPIPersister->>RapidAPIService: rapidAPIfetch(searchWord, is_auth)
            activate RapidAPIService
            RapidAPIService->>ProcessRapidAPI: normalize each job
            ProcessRapidAPI-->>RapidAPIService: processed job (or null)
            RapidAPIService-->>RapidAPIPersister: fetchedJobs[]
            deactivate RapidAPIService
            Note over RapidAPIPersister: Persists in background via persistJobSearch
            RapidAPIPersister-->>PersistJobSearch: async persistJobSearch(fetchedJobs, searchWord, is_auth)
            deactivate RapidAPIPersister
            JobController->>JobController: add fetchedJobs to aggregatedJobs (dedupe by id)
        end
    end
    JobController-->>FrontendNetwork: HTTP 200 { success: true, result: aggregatedJobs }
    deactivate JobController
```
