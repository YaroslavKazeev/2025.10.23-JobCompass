**Phase 6: Display Results**

- [← Back to Job Fetch Flowchart](_JobFetchFlowchart.md)
- [← Previous: Multiple possible paths]<br/>- [Phase 4.2: Client-Side Error Handling](4.2.Client-Side%20Error%20Handling.md)<br/>- [Phase 4.3.2: Full Search String Cached](4.3.2.Full%20Search%20String%20Cached.md)<br/>- [Phase 4.3.3: Not Cached for Full String](4.3.3.Not%20Cached%20for%20Full%20String.md)<br/>- [Phase 4.4: Server-Network Error Handling](4.4.Server-Network%20Error%20Handling.md)<br/>- [Phase 5: Travel Details Fetch](5.Travel%20Details%20Fetch.md)
- [Next] - (This is the final phase)

```mermaid
sequenceDiagram
    autonumber
    actor User
    %% File paths:
    %% JobsContext: client/src/context/JobsContext.jsx
    %% OpenPositions: client/src/pages/openPositions/OpenPositions.jsx
    %% JobCard: client/src/components/JobCard/JobCard.jsx
    participant JobsContext as JobsContext Provider<br/>JobsContext.jsx
    participant Router as React Router<br/>(client-side navigation)
    participant OpenPositions as OpenPositions Page<br/>OpenPositions.jsx
    participant LoaderOverlay as Loader Overlay<br/>(loading GIF)
    participant ErrorDisplay as Error Message Div<br/>(.error-message)
    participant JobCard as JobCard Component<br/>JobCard.jsx

    Note over User,ErrorDisplay: Phase 6: Display Results
    activate JobsContext
    JobsContext->>JobsContext: getJobsWithTravel(): merge travel_time & least_transfers<br/>from travelDetails map into each job
    deactivate JobsContext

    Router->>OpenPositions: Route change complete,<br/>component renders

    activate OpenPositions
    OpenPositions->>JobsContext: Read from context
    activate JobsContext
    OpenPositions->>JobsContext: allJobs, searchString, isJobsLoading,<br/>jobFetchError, travelFetchError, serverMessage
    deactivate JobsContext
    deactivate OpenPositions

    OpenPositions->>OpenPositions: useEffect alert handling:
        - if jobFetchError -> AlertMessage(type: error)
        - else if travelFetchError -> AlertMessage(type: error)
        - else if serverMessage -> AlertMessage(type: info) then clear serverMessage
        - auto-dismiss after 5 seconds

    alt isJobsLoading is true
        activate OpenPositions
        OpenPositions->>LoaderOverlay: Render loading overlay<br/>with boat GIF animation
        activate LoaderOverlay
        LoaderOverlay->>User: Display loading animation
        deactivate LoaderOverlay
        deactivate OpenPositions
        Note over OpenPositions,User: All other content is overlaid<br/>by loading screen
    else jobFetchError or travelFetchError exists
        activate OpenPositions
        OpenPositions->>ErrorDisplay: Render AlertMessage with error text
        ErrorDisplay-->>User: "Error loading jobs or commute info: [error]"
        deactivate OpenPositions
    else Loading complete & no errors
        activate OpenPositions
        OpenPositions->>OpenPositions: Enrich jobs with skills:<br/>For each job & user skill,<br/>call `getSkillsInDescription()`<br/>to find matching skills

        OpenPositions->>OpenPositions: Apply filters from user selection<br/>using `filterJobs()`<br/>(seniority, employment type, work mode)

        OpenPositions->>OpenPositions: Apply sorting from dropdown<br/>using `createSortComparator()`<br/>(skill matches, transfers, distance, date)

        OpenPositions->>OpenPositions: Paginate results<br/>(5 jobs per page)

        alt filteredJobs.length === 0
            OpenPositions->>User: Display no results message:<br/>"No jobs are shown. Go to Job search<br/>or Clear filters to see more results."
        else filteredJobs.length > 0
            OpenPositions->>OpenPositions: Render job count text<br/>("Found [allJobs.length] jobs..." + filtered count when filters active)
            OpenPositions->>OpenPositions: Render job cards list (current page)
            OpenPositions->>JobCard: Pass job + favorites + isTravelLoading
            JobCard-->>User: Show travel spinner while isTravelLoading; otherwise travel_time/least_transfers when present
            OpenPositions-->>User: Pagination controls + filters/sorts
        end
        deactivate OpenPositions
    end
```
