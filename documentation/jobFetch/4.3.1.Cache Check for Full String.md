**Phase 4.3.1: Cache Check for Full String**

- [← Back to Job Fetch Flowchart](_JobFetchFlowchart.md)
- [← Previous: Phase 4.1: Request Validation & Initiation](4.1.Request%20Validation%20&%20Initiation.md)
- [Next → Phase 4.3.2: Full Search String Cached](4.3.2.Full%20Search%20String%20Cached.md)
- [Alternative → Phase 4.3.3: Not Cached for Full String](4.3.3.Not%20Cached%20for%20Full%20String.md)

```mermaid
sequenceDiagram
    autonumber
    %% File paths:
    %% JobController: server/src/controllers/jobData.js
    %% CachedJobsService: server/src/services/getCachedJobsBySearchString.js
    %% NeonDB: Neon Database
    participant JobController as Job Search Controller<br/>jobData.js
    participant CachedJobsService as getCachedJobsBySearchString<br/>getCachedJobsBySearchString.js
    participant NeonDB as Neon Database<br/>search_strings / search_strings_jobs / jobs

    Note over JobController,NeonDB: Phase 4.3.1: Cache Check for Full String
    activate JobController

    JobController->>CachedJobsService: getCachedJobsBySearchString(connectedClient,<br/>search_string.toLowerCase(), is_auth)
    activate CachedJobsService
    CachedJobsService->>NeonDB: SELECT search_string, is_whole_string<br/>FROM search_strings WHERE search_string = $1
    activate NeonDB
    alt Row exists
        NeonDB-->>CachedJobsService: is_whole_string flag
        CachedJobsService->>NeonDB: JOIN search_strings_jobs + jobs<br/>WHERE search_string = $1 AND ($2::uuid IS NULL OR ss.is_auth IS NOT NULL)
        NeonDB-->>CachedJobsService: Return cached job rows
    else No match
        NeonDB-->>CachedJobsService: Empty result (no cache)
    end
    deactivate NeonDB
    CachedJobsService-->>JobController: { is_whole_string, cachedJobsPerSearchString }
    deactivate CachedJobsService
    deactivate JobController
```
