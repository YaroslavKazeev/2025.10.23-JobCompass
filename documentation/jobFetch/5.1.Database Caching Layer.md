**Phase 5.1: Database Caching Layer**

- [← Back to Job Fetch Flowchart](_JobFetchFlowchart.md)
- [← Previous: Phase 4.3: Search Processing & Results Aggregation](4.3.Search%20Processing%20&%20Results%20Aggregation.md)
- [Next → Phase 6: Response Delivery](6.Response%20Delivery.md)

```mermaid
sequenceDiagram
    autonumber
    %% File paths:
    %% CacheService: server/src/services/getCachedJobsBySearchString.js
    %% PersistService: server/src/services/persistJobSearch.js
    %% Database: Neon PostgreSQL Database
    participant CacheService as getCachedJobsBySearchString.js
    participant PersistService as persistJobSearch.js
    participant Database as Neon PostgreSQL

    Note over CacheService,Database: Phase 5.1: Database Caching Layer

    %% Cache Retrieval Flow
    activate CacheService
    CacheService->>Database: SELECT search_string, is_whole_string<br/>FROM search_strings WHERE search_string = $1
    activate Database
    Database-->>CacheService: Return is_whole_string (or none)
    deactivate Database

    alt Cache hit
        CacheService->>Database: SELECT j.* FROM jobs j<br/>JOIN search_strings_jobs swj ON j.id = swj.job_id<br/>JOIN search_strings ss ON swj.search_string = ss.search_string<br/>WHERE swj.search_string = $1 AND ($2::uuid IS NULL OR ss.is_auth IS NOT NULL)
        activate Database
        Database-->>CacheService: Cached job rows
        deactivate Database
        CacheService-->>Caller: { is_whole_string, cachedJobsPerSearchString }
    else No cache hit
        CacheService-->>Caller: { is_whole_string: false, cachedJobsPerSearchString: [] }
    end
    deactivate CacheService

    %% Persistence Flow (async)
    Note over PersistService,Database: Background persistence started by RapidAPI/Apify persisters
    activate PersistService
    PersistService->>Database: INSERT search_strings (search_string, search_date, is_auth[, is_whole_string])<br/>ON CONFLICT UPDATE search_date, is_auth, is_whole_string
    activate Database
    Database-->>PersistService: search_strings upserted
    deactivate Database

    loop For each job that is complete & posted within last 30 days
        PersistService->>Database: INSERT jobs (...) VALUES (...)<br/>ON CONFLICT (id) DO NOTHING
        activate Database
        Database-->>PersistService: Job saved or skipped
        deactivate Database

        PersistService->>Database: INSERT search_strings_jobs(search_string, job_id)<br/>ON CONFLICT DO NOTHING
        activate Database
        Database-->>PersistService: Link created
        deactivate Database
    end
    deactivate PersistService
```

## Technical Implementation Details

### Database Behavior Notes

- **Auth filter**: getCachedJobsBySearchString uses `($2::uuid IS NULL OR ss.is_auth IS NOT NULL)`, so guest searches can reuse any cached string; authenticated searches read entries where `is_auth` is not null (not scoped per user).
- **Completeness check**: persistJobSearch skips jobs with null fields (except travel_time, least_transfers, work_mode) and skips posts older than 30 days.
- **is_whole_string flag**: When persisting a full-string scrape, `is_whole_string` is stored to avoid retriggering Apify for the same exact phrase.
- **Conflict handling**: Jobs and search-string links use `ON CONFLICT DO NOTHING`; search_strings updates last seen timestamp and auth flags on conflict.
